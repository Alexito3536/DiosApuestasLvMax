name: Scraper Dios-T Automático
on:
  schedule:
    - cron: '0 9 * * *' # Corre a las 9 AM todos los días
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  run-scraper:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Instalar Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'
      - name: Instalar Chrome y Selenium
        run: |
          pip install pandas scikit-learn numpy selenium webdriver-manager
      - name: Ejecutar el Dios de las Apuestas
        run: python script_dios.py
      - name: Actualizar Web
        run: |
          git config --global user.name 'Dios-T-Bot'
          git config --global user.email 'bot@dios.t'
          git add data.json
          git commit -m "🔍 Datos frescos extraídos del mercado" || exit 0
          git push

